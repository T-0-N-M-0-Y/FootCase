const Offer = () => {

    return (
        <div>
            <h1 className="text-center bg-red-600 text-white">TodaY Offer: $20 OFF order $300 or more with code `Juta-002` + Free Shipping on order over $60 !</h1>
        </div>
    );
};

export default Offer;